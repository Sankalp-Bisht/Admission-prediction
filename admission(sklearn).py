# -*- coding: utf-8 -*-
"""Admission(sklearn).ipynb

Automatically generated by Colaboratory.

Original file is located at
    https://colab.research.google.com/drive/1O9wOaVkW7ZAvuhqhtOcwGitScQVLS_g0
"""

import pandas as pd
import numpy as np
import matplotlib.pyplot as plt
import sklearn
import math

from pylab import rcParams

dataset=pd.read_csv('Admission_Predict.csv')
dataset.head(5)

import sklearn as sk
from sklearn.model_selection import train_test_split
from sklearn import linear_model as lr
from sklearn.metrics import mean_squared_error, r2_score

X=dataset.drop(['Chance of Admit ','Serial No.'],axis=1)
X["X1"]=1
y=dataset['Chance of Admit ']

X_train, X_test, y_train, y_test = train_test_split(X, y, test_size=0.2, random_state=0)



r= lr.LinearRegression()

r.fit(X_train,y_train)

coeff_df = pd.DataFrame(r.coef_, X.columns, columns=['Coefficient'])
print(coeff_df)

y_pred = r.predict(X_test)

r2_score(y_test,y_pred)

x1=dataset['TOEFL Score']
x2=dataset['GRE Score']
plt.plot(x1,y,'o')